module.exports = {
	ci: {
		collect: {
			staticDistDir: './build',

			/*
			 * Адреса вказана ЯВНО, і це не уточнення, а виправлення.
			 *
			 * Без неї LHCI сам шукає HTML у `staticDistDir`, а в `build/` лежить
			 * і `404.html` — SPA-фолбек без пререндереного вмісту, який до того ж
			 * не вміє завантажитися з кореня сервера, коли застосунок живе за
			 * префіксом base. Разом із `maxAutodiscoverIsolate: 1` вибір падав
			 * саме на нього: Chrome не малював жодного кадру, Lighthouse падав із
			 * `NO_FCP`, а крок стоїть ПЕРЕД викладенням артефакту — тобто гейт,
			 * який жодного разу не проходив, блокував увесь деплой.
			 *
			 * Хост тут фіктивний: LHCI піднімає власний сервер на випадковому
			 * порті й підставляє його origin, беручи з цього рядка лише шлях.
			 *
			 * ── ОДНА АДРЕСА, А НЕ ПЕРЕЛІК ІЗ МАРШРУТІВ — СВІДОМЕ ЗВУЖЕННЯ.
			 *
			 * `OBS-LHCI-REAL-PAGES` (MEDIUM) велить генерувати перелік із того
			 * самого джерела, що й sitemap. Тут сторінок 85, і Lighthouse на
			 * кожній — це хвилини CI на кожен пуш, тоді як три з чотирьох його
			 * категорій уже покриті ширше й дешевше: доступність — axe над
			 * сторінками й накладками (`npm run test:e2e`), SEO і структура
			 * `<head>` — інваріантами над усіма 85 сторінками у `check-build.mjs`.
			 * Лишається продуктивність, і вона тут визначається спільним
			 * бандлом, а не окремим маршрутом; його стереже бюджет у тому ж
			 * `check-build.mjs`.
			 *
			 * ЩО ЦЕ ЗВУЖЕННЯ КОШТУЄ: нова сторінка в замір не потрапляє. Знати
			 * це важливо саме тому, що виглядає воно як покриття.
			 *
			 * Друга половина правила — канарка «сторінка справді вантажить CSS
			 * і JS» — НЕ звужена: вона стоїть у `check-build.mjs` і читає цей
			 * перелік прямо звідси, тож розійтися вони не можуть. Саме та
			 * канарка ловить дефект, описаний вище: адресу, чиї активи не
			 * розв'язуються від кореня `build/`.
			 */
			url: ['http://localhost/index.html'],
			numberOfRuns: 1
		},
		assert: {
			assertions: {
				'categories:performance': ['warn', { minScore: 0.8 }],
				'categories:accessibility': ['error', { minScore: 0.95 }],
				'categories:best-practices': ['error', { minScore: 0.9 }],
				'categories:seo': ['error', { minScore: 0.9 }]
			}
		},
		upload: {
			target: 'temporary-public-storage'
		}
	}
};

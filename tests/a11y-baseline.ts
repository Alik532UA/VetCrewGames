/**
 * База для axe-аудиту (ACCESSIBILITY-v8 § 10.1.1, `A11Y-AXE-BASELINE`).
 *
 * ## ЧОМУ ЧИСЛО, А НЕ `toEqual([])`
 *
 * Нуль виглядає найсуворішим порогом і насправді є найкрихкішим: `axe-core`
 * додає правила в МІНОРНИХ версіях, а Dependabot приносить їх щотижня. Гейт із
 * нулем червоніє від оновлення залежності без жодної зміни коду — а червоний
 * гейт, причина якого не в коді, вимикають. Це той самий клас, що `off` у
 * ESLint: поріг, який доводиться вимкнути назавтра, гірший за відсутній.
 *
 * Тому база — це ПАРА:
 *   KNOWN — перелік id правил. Поява НОВОГО типу порушення валить тест навіть у
 *           межах ліміту, тож борг не ховається за числом;
 *   COUNT — кількість. Може лише спадати, і кожне зменшення робиться разом із
 *           правкою, що його спричинила.
 *
 * ## КЛЮЧ — ЦЕ АДРЕСА СТОРІНКИ, і це не косметика
 *
 * Доти ключів було три (`home`, `homeDark`, `betaChecklist`) — рівно стільки,
 * скільки сторінок бачив axe. Перелік сторінок при цьому в проєкті вже був
 * (`tests/support/pages.ts`, чотирнадцять адрес), ним ходили замір контрасту й
 * перевірка унікальності локаторів, і його докблок називає вибірковість
 * найдорожчим дефектом контрасту тут: «axe ходив на ДВІ сторінки з одинадцяти,
 * там зашитий білий колір виправили, а на сторінках ігор той самий колір прожив
 * далі».
 *
 * Тепер ключ виводиться з адреси (`keyFor` у `a11y.spec.ts`), тож нова сторінка
 * в `APP_PAGES` одразу вимагає свого рядка тут — і не може тихо лишитися поза
 * аудитом. Рядок, якому не відповідає жодна сторінка, так само валить прогін:
 * прострочений виняток ховає наступне порушення того самого файлу.
 *
 * ## Числа отримані ПРОГОНОМ, а не оцінені
 *
 * `npm run test:e2e -- tests/a11y.spec.ts` на зібраному сайті
 * (AI-AGENT-PITFALLS-v8 § 5.5: число зі звіту старіє саме тоді, коли робота йде
 * добре — тому воно тут одне, і команда, якою його отримати, названа).
 * Заміряно 2026-08-27.
 */

/** Типи порушень, які вже є. Новий id — падіння, навіть якщо кількість не зросла. */
export const A11Y_KNOWN: Record<string, readonly string[]> = {
	home: [],
	'home@dark': [],
	'game-family': [],
	/*
	 * ЗНАЙДЕНО ЦИМ ГЕЙТОМ, 2026-08-27, першим же прогоном по всіх сторінках.
	 *
	 * Чотири зони «кому віддати» (`FeedingZone`, зокрема смітник) і сам стіл
	 * (`FeedingTable`) — це `div` із `role="button"` і `tabindex="0"`, усередині
	 * яких лежать СПРАВЖНІ кнопки: мініатюри-натяки (`FeedingHints`) і покладені
	 * страви (`FeedingPlated`). Для скрінрідера це кнопка всередині кнопки: WCAG
	 * 4.1.2 (рівень A), і читалка озвучує зовнішній підпис замість внутрішнього.
	 *
	 * Це НЕ дрібна правка атрибута: зона водночас є ціллю кидка, ціллю кліку по
	 * всій площі (`ACCESSIBILITY-v8` § 2 — на сенсорному екрані HTML5 drag не
	 * працює взагалі, тож клік основний) і контейнером кнопок. Правильна форма —
	 * зона як група з окремою кнопкою «покласти сюди», і це переробка взаємодії
	 * у грі, де половина шляхів — пальцем. Тому борг записаний числом, а не
	 * закритий наосліп.
	 */
	'game-feeding': ['nested-interactive'],
	'game-habitat': [],
	'game-memory': [],
	'game-mythbusters': [],
	/*
	 * ЗНАЙДЕНО ЦИМ ГЕЙТОМ, 2026-08-27. Два id у звіті, але ПРИЧИНА ОДНА, і це
	 * варто знати до того, як хтось візьметься правити не те.
	 *
	 * `nested-interactive` — той самий клас, що в «Що їмо?»: `role="button"` і
	 * `tabindex="0"` на контейнері `.game-container[data-source-index]`, усередині
	 * якого лежить фокусований вміст.
	 *
	 * `target-size` (WCAG 2.2 SC 2.5.8) axe пише на ТИХ САМИХ трьох елементах, і
	 * читається це оманливо: «insufficient size because it is partially obscured
	 * (smallest space is 2px by 170px)». Схоже на «картки стоять упритул» — і це
	 * НЕ ТАК. Заміряно на 390×844: картки 110×170 із проміжком 6px (x = 24, 140,
	 * 256), тобто самі цілі вчетверо більші за потрібні 24×24 і не перекривають
	 * одна одну. Два пікселі — це те, що лишилося від контейнера НЕЗАКРИТИМ його
	 * ВЛАСНОЮ фокусованою дитиною: axe рахує перекриття цілі ціллю. Отже це не
	 * другий дефект, а тінь першого, і зникне воно разом із ним.
	 *
	 * ЩО ТУТ НЕ ТРЕБА РОБИТИ: розсувати картки або міняти розкладку — це не
	 * полагодить нічого, а на телефоні ще й забере місце в самих карток.
	 *
	 * Лікується разом із розбором `PopulationBoard` (`src/structure.test.ts`,
	 * `OVERSIZED_ALLOWLIST`): там уже названо, що керування вводом — мишу, палець,
	 * клік, подвійний клік — треба винести в контролер. Робити це наосліп не можна:
	 * поруч лежить перетягування пальцем, де тиха помилка спливе не в тесті, а в
	 * людини на телефоні.
	 */
	'game-population': ['nested-interactive', 'target-size'],
	quiz: [],
	reserve: [],
	pairs: [],
	'pairs/online': [],
	'quiz/online': [],
	account: [],
	'beta-test-checklists': []
};

/** Кількість порушень. Рухається лише вниз. */
export const A11Y_BASELINE: Record<string, number> = {
	home: 0,
	'home@dark': 0,
	'game-family': 0,
	// Дві зони тварин, смітник і стіл — по одному вузлу на кожен.
	'game-feeding': 4,
	'game-habitat': 0,
	'game-memory': 0,
	'game-mythbusters': 0,
	// Три картки-джерела × два правила (`nested-interactive` і `target-size`).
	'game-population': 6,
	quiz: 0,
	reserve: 0,
	pairs: 0,
	'pairs/online': 0,
	'quiz/online': 0,
	account: 0,
	'beta-test-checklists': 0
};

.svelte-kit
build
node_modules
package-lock.json
static/app-version.json
# Спільна таблиця сусідніх сайтів: той самий файл лежить у восьми репозиторіях,
# і форматується він за конфігом того, звідки походить (adoptananimal). Дати
# тутешньому prettier переписати його означало б діфф на кожній синхронізації
# там, де вміст не змінився.
src/lib/siblings.ts

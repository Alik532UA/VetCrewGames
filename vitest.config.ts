import { defineConfig } from 'vitest/config';
import { svelte } from '@sveltejs/vite-plugin-svelte';
import { fileURLToPath } from 'node:url';

export default defineConfig({
	// Без `hot`: опції з такою назвою в плагіні більше немає, і кожен прогін
	// тестів починався з рядка `invalid plugin options "hot" in inline config`.
	// Попередження, яке бачать сто разів на день, навчає не читати вивід —
	// а саме у виводі й лежить те, заради чого тести запускають.
	plugins: [svelte()],
	resolve: {
		conditions: ['browser'],
		alias: {
			$lib: fileURLToPath(new URL('./src/lib', import.meta.url)),
			'$app/paths': fileURLToPath(new URL('./src/lib/mocks/app-paths.ts', import.meta.url)),
			'$app/environment': fileURLToPath(
				new URL('./src/lib/mocks/app-environment.ts', import.meta.url)
			)
		}
	},
	test: {
		include: ['src/**/*.{test,spec}.{js,ts}'],
		environment: 'jsdom',
		globals: true,
		// Мережа в юніт-тестах заблокована, а тест, що до неї дійшов, падає з
		// адресою: без цього незамоканий `connect()` реєстрував акаунт у живому
		// Firebase (`src/no-network.setup.ts`).
		setupFiles: ['src/no-network.setup.ts'],
		/**
		 * МЕЖА ЧАСУ МІРЯЄ КОД, А НЕ ЗБІРНИК — і типові 5 секунд міряли збірник.
		 *
		 * Знайдено прогоном, а не міркуванням. `npm test` падав приблизно через
		 * раз, і завжди на тих самих чотирьох перевірках у трьох файлах:
		 * `log-context`, `i18n-completeness`, `playerSync` (двічі). Спільне в них
		 * одне — кожна першою у своєму воркері робить `await import('$lib/i18n')`,
		 * а це чотири повні словники.
		 *
		 * Заміряно з піднятою межею, і числа не лишають місця для тлумачень:
		 *
		 *     перша перевірка, що торкнулася словників   8388 мс і 8582 мс
		 *     кожна наступна в тому самому файлі         15–41 мс
		 *     `log-context`                              5252 мс — рівно на межі
		 *
		 * Тобто типова межа витрачалася на трансформацію модулів, а не на код, який
		 * перевіряють: та сама перевірка після прогріву кешу йде за 15 мс.
		 * `log-context` при 5252 мс і був тим, що робило прогін плаваючим —
		 * зелений на вільній машині, червоний під навантаженням.
		 *
		 * Плаваючий гейт гірший за відсутній: на нього перестають дивитися, а далі
		 * перезапускають, доки не позеленіє (AI-AGENT-PITFALLS-v8 § 1.1). Саме
		 * цим міркуванням уже піднято межу в `eslint-baseline.test.ts`.
		 *
		 * 30 секунд — це 3,5× від найгіршого заміряного, із запасом на раннер CI,
		 * який повільніший за цю машину. Ціна названа: справді зависла перевірка
		 * тепер тримає воркер 30 секунд замість 5. Це прийнятно — увесь набір іде
		 * близько хвилини, а зависання і так треба бачити, а не швидко ховати.
		 */
		testTimeout: 30_000,
		/**
		 * CODE-QUALITY-v8 § 6.2 — поріг покриття критичної логіки.
		 *
		 * Доти цей блок був оголошенням, а не гейтом: `npm test` не передавав
		 * `--coverage`, провайдера в залежностях не було, і поріг не тримав нічого.
		 * Стан був чесно записаний у PROJECT-CONTEXT.md як відхилення — саме тому
		 * його й було видно, і саме тому його закрито.
		 *
		 * `include` розширено з `services/**` до пари «контролери + сервіси», як і
		 * приписує канон. Це не послаблення: контролери — найбільша частина логіки
		 * проєкту, у кожного є тест поруч, і без них середнє рахувалося по семи
		 * файлах замість двадцяти.
		 *
		 * Поріг 70 — число канону, а не виміряне «щоб пройшло». Виміряно на момент
		 * увімкнення (`npx vitest run --coverage`, 2026-08-18): statements 84,62%,
		 * branches 75,77%, functions 87,78%, lines 86,32%. Запас є, і поріг лишається
		 * порогом, а не фотографією поточного стану.
		 */
		coverage: {
			include: ['src/lib/controllers/**', 'src/lib/services/**'],
			thresholds: { statements: 70, branches: 70 }
		}
	}
});

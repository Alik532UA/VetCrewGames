# Контекст проєкту: VetCrewGames

Персональний шар над пакетом v8 (`sveltekit-canon/selection_criteria/v8`).

## Базові параметри

| Параметр | Значення |
|---|---|
| Профіль | **static** |
| Adapter | `@sveltejs/adapter-static` |
| Хостинг | GitHub Pages, project page акаунта `Alik532UA` |
| Origin | `https://alik532ua.github.io` |
| base path | `/VetCrewGames` у production, `''` у dev |
| Спільний origin з іншими застосунками? | **так** |
| PROJECT_PREFIX | `vetcrewgames_` |
| Node | **22** — однаково у `engines.node`, `.nvmrc` і workflow; звіряється інваріантом у `src/ci.test.ts` |
| Бекенд | Firebase Realtime Database + анонімний Auth, лише для спільної партії «Знайди пару». Firestore **не використовується** |
| Файли правил доступу | `database.rules.json`, прив'язаний через `firebase.json` |
| Порти емулятора | database **9010**, auth **9109**, UI **4010** — навмисно не типові, бо на цій машині сусідній `MindStep` займає 9000/9099/8080 |

## Хмарна база: стан правил доступу

Заповнено за `CLOUD-DATABASE-v8` § 2.3. Правила — **єдина** межа безпеки цієї
частини застосунку: публічний конфіг Firebase за побудовою лежить у бандлі, тож
адресу бази знає кожен, хто відкрив сайт.

| Питання | Відповідь |
|---|---|
| Правила лежать у репозиторії? | так — `database.rules.json` |
| `firebase.json` прив'язує їх? | так |
| Гейт над емулятором | `npm run check:rules` — **26 перевірок: 9 дозволено, 17 заборонено** (число з прогону 2026-08-17, не з пам'яті) |
| Гейт у CI? | так, окремий джоб `rules` у `deploy.yml` із `setup-java` |
| Викладання | **руками**: Firebase Console → Realtime Database → Rules. `firebase deploy --only database` теж працює, але сервісного акаунта в CI немає |
| Стан консолі звірений із файлом? | **НЕ звірений.** У файлі правила СТРОГІШІ за викладені: додано обов'язковий серверний `at` у ході, `startedAt` у кімнаті й рядки в `payload`. Доки не викладено — спільна гра писатиме ходи, які поточна консоль пропустить, а нова версія клієнта чекатиме на поля, яких старі правила не вимагають |
| Список дозволених доменів звірений? | не звірений |
| Публічний конфіг постачається (SECURITY § 4.1) | **у джерелі** (`src/lib/net/firebase.ts`) — вибір за правилом, не відхилення. Значення однакове для всіх середовищ і однаково публічне; змінна оточення додала б крок, на якому збірка Pages тихо збереться з порожнім конфігом, і онлайн зламався б без жодної помилки |

### Що зробити при першій же нагоді

1. **Викласти `database.rules.json` у консоль** і замінити рядок вище на дату
   звірки. Доки там стоїть «не звірений», твердження «правила налаштовані»
   робити не можна: воно описує намір, а не факт.
2. Після цього — прогнати спільну партію на двох пристроях: створити кімнату,
   зайти за кодом, зробити по ходу кожним. Гейт над емулятором доводить, що
   правила правильні; він не доводить, що вони викладені.

### Межі бази, до яких проєкт наблизився

| Межа | Де саме | Поточний стан | Що станеться при досягненні |
|---|---|---|---|
| Кількість кімнат | `rooms/{code}` | код — 5 літер з 32-символьного алфавіту, тобто ~33 млн комбінацій | `createRoom` робить 5 спроб і чесно кидає `room-code-taken`; за реальної кількості кімнат зіткнення практично неможливе |
| Покинуті кімнати | `rooms/{code}` | кімната в лобі зникає разом із вкладкою господаря (`onDisconnect`); почата партія лишається до явного «Закрити кімнату» | накопичуються лише партії, які почали й покинули. Кілобайт кожна; **клієнтської зачистки навмисно немає** — вона вимагала б права видаляти чуже, а це заразом примітив «видалити всі кімнати» |
| Журнал ходів | `rooms/{code}/moves` | одна партія — десятки ходів по ~60 байтів | межі немає: ходи — окремі діти, а не поле, тож ні розмір документа, ні «1 запис/с» тут не діють |

## Звірені факти

Тут лежать твердження, які **читає інваріант**, а не лише людина:
`src/project-context.test.ts` бере кожен рядок і звіряє його з джерелом істини в
останній колонці. Розійтися з кодом вони більше не можуть.

Перелік навмисно короткий. Сюди йде не все підряд, а рівно ті факти, від яких
залежить, **які файли пакета взагалі застосовні** й де ціна помилки — чужі дані.
Числа, що змінюються від кожного коміту (кількість тестів, версія), сюди не
йдуть: гейт, який червоніє від зростання проєкту, гірший за відсутній.

<!-- FACTS:BEGIN — рядки звіряє src/project-context.test.ts; ID міняються разом із ним -->

| ID                | Значення                          | Джерело істини                              |
| ----------------- | --------------------------------- | ------------------------------------------- |
| `languages`       | `uk, en, de, nl`                  | `LANGUAGES` у `src/lib/i18n/routing.ts`     |
| `storage-prefix`  | `vetcrewgames_`                   | `PREFIX` у `src/lib/services/storage.ts`    |
| `emulator-ports`  | `database 9010, auth 9109, ui 4010` | `emulators` у `firebase.json`             |
| `adapter`         | `@sveltejs/adapter-static`        | `svelte.config.js`                          |
| `text-inputs`     | `є`                               | `<input>` і `<textarea>` у `src/`           |

<!-- FACTS:END -->

Останній рядок виглядає дрібницею й нею не є: саме на «полів вводу тут немає»
трималося рішення не застосовувати `FORM-INPUTS` та `INPUT-TOOLS`. Поля
з'явилися разом зі спільною партією, а рядок лишився — і два файли пакета
випадали з розгляду на підставі, якої вже не існувало.

## Реєстр префіксів на спільному origin

| Застосунок | Префікс |
|---|---|
| VetCrewGames | `vetcrewgames_` |
| CV | `cv-svelte_` |
| DigitalWorkshop | `digitalworkshop_` |
| as5.odesa.ua | `as5.odesa.ua_` |
| MindStep | `mindstep_` |
| Slovko | `slovko_` |

## Прийняті рішення

| Питання | Обрано | Причина | Дата |
|---|---|---|---|
| Бібліотека i18n | **власна** (`lib/i18n/index.ts` + словники `.ts`) | чотири мови — `uk`, `en`, `de`, `nl`; паритет тримає інваріант `translations.test.ts`, а повноту трьох кроків увімкнення — `i18n-completeness.test.ts` | до 2026-08 |
| Концепції ігор | лежать поза цим репозиторієм — `adoptananimal/.private/init/` | Там опис усіх п'яти ігор і дорожня карта. У самому проєкті від нереалізованих ігор лишалися три рядки в словниках, тож наступний, хто шукатиме «а що це мало бути», має знати, куди дивитися | 2026-08-16 |
| Перетягування у «Що їмо?» | **клік як основний шлях**, HTML5 drag — надбудова | Концепція каже «drag-and-drop», але на сенсорних екранах він не працює взагалі, а з клавіатури — тим паче. Тому основний шлях «взяти страву, клікнути ціль», і саме він покритий тестами. Клон-під-пальцем із гри про чисельність тут не повторюється: там перетягування — сама механіка, тут — зручність. Брати можна й те, що вже лежить у зоні, — інакше зміна рішення коштує два кроки | 2026-08-16 |
| Об'ємність картки у «Кого більше?» | живе на `.game-container`, а не на `.game-card` | Картка заповнює слот рівно, тож підйом, `scale` і тверда тінь виносили її кут за скруглення слота. Обрізанням не лікується — картка літає між слотами через `crossfade`. Стереже інваріант «картка не рухається за межі свого слота» | 2026-08-16 |
| Кнопки «кому віддати» у «Що їмо?» | **дві тварини**, без смітника | Смітник і так на всю ширину під столом, тобто вже в один клік, а третя кнопка не вміщується: страві дістається 43–59px залежно від розкладки, ряд із трьох там не живе | 2026-08-16 |
| Розкладка «Що їмо?» | три колонки на будь-якій ширині, медіазапиту немає | Поки страви стояли на столі в ряд, три по 44px вимагали 410px вікна, і нижче розкладка мусила ставати стовпчиком. Відколи стіл вертикальний, йому досить 76px — три колонки вміщуються навіть у 320px, і перемикати нема чого | 2026-08-16 |
| Текст на підкладці | у кожного тексту фон — свій або предків | Тло застосунку — фотографія з довільними світлими й темними ділянками, і текст просто на ній читається як пощастить. «Роздай страви тваринам» і «Де ця тварина живе» стояли так місяцями, і побачив це користувач, а не гейт. Спільний клас `.text-panel` у `global.css`, стереже інваріант `src/backdrop.test.ts` | 2026-08-16 |
| Значок правильно викинутої страви | зелений трикутник, а не галочка | Гравець вгадав — але вгадав НЕБЕЗПЕКУ: шоколад, цибулю, хліб для качок. Галочка гасила б саме те, заради чого гра й існує. Умова спільна з текстом попередження, щоб значок і пояснення не розійшлися | 2026-08-16 |
| Смуга прокрутки | режим `custom` за замовчуванням, `standard` — на вибір правою кнопкою | З трьох вимог SCROLLBAR § 1 тут стоїть третя: смуга товщає при наближенні миші, а нативній це недоступно в принципі. Прокрутник — не сторінка, а `.page-transition-wrapper`, тож він публікується явно. На сенсорних екранах і без точного вказівника лишається системна | 2026-08-16 |
| Підтвердження в «Що їмо?» | досить одного ходу; лишене на столі = смітник | Страву на столі й так рахували як викинуту (`?? BIN` у розборі), тож вимога покласти всі три була не правилом гри, а зайвим рухом після вже прийнятого рішення. Один хід усе-таки потрібен: інакше кнопка активна з першої секунди й випадковий дотик коштує раунду | 2026-08-16 |
| Кнопки «кому віддати» | тварини по боках страви, смітник під нею; суперeліпс | Стоять там, де на екрані стоять самі зони. Бокові — абсолютно, поза потоком: у потоці вони диктували б ширину страві, а колонці столу її й так обмаль. Форма — `clip-path` на 28 точок, бо `corner-shape` дає попередження `svelte-check`, а проєкт тримає нуль | 2026-08-16 |
| Розбір у «Що їмо?» | ЗБОКУ від тварини, якій страва належить (від 900px); нижче — під нею | Один стовпчик пояснень унизу тягнув сторінку в прокрутку, поки боки стояли порожні. Розкладка по `correct`, а не по `chosen`: пояснення стоїть біля того, про кого воно й розповідає. Три розкладки на одній сітці, і перемикають їх іменовані області, а не різна розмітка: DOM однаковий завжди. Від 900px розбір іде в поля, які доти простоювали (колонці лишається 237px на 1280); від 640 до 900 — під твариною; нижче 640 все стовпчиком, бо колонці лишається менше за 200px і текст стає стовпчиком по слову | 2026-08-16 |
| Залишки на столі | їдуть у смітник самі, у момент «Погодувати» | Рахувалися вони так і раніше, але на екрані лишалися на столі — гравець бачив стан, якого вже немає. Заразом це звільняє стіл, і в порожній контейнер стає кнопка «Далі»: по неї більше не треба тягнутися вниз повз увесь розбір | 2026-08-16 |
| Варіанти у «Де живем?» | від 1000px — ОДИН ряд і ширша сторінка; зображення над текстом від 700px, нижче маленьке перед ним | Дев'ять природних зон картками на всю ширину дали б на телефоні екран заввишки в кілька прокруток. Поріг 700px — там сторінка вже впирається у свою стелю 560px, тобто ширшати нема куди, і колонок стає чотири. Стеля 84px на зображення: при 3:4 на всю колонку воно було б заввишки 227px. Ряд — `auto-flow: column`, а не сітка: варіантів завжди 7 або 9, і `auto-fit` при восьми колонках лишає сироту в другому ряду. Поріг 1000px узятий із ширини: щоб дев'ятьом лишалося хоч по 96px, сторінці треба 928 | 2026-08-16 |
| «Назад» у шапці | сторінка може перехопити його через `settings.setHeaderBack()` | У «Де живем?» спершу треба повернутися до вибору режиму, а вже звідти — з гри: інакше перемкнути континенти на зони можна було лише через головну. Лишається посиланням на головну, а не стає кнопкою — середній клік, Ctrl-клік і робота без JS зберігаються; перехоплюється тільки звичайний клік | 2026-08-16 |
| «Знайди пару» | написана під СПІЛЬНУ партію з першого дня, хоча грає один | Дописати мультиплеєр у гру, яка про нього не знає, означає переписати її: гравець із «я» стає списком, черга з'являється там, де її не було, очко раптом комусь належить. Тому вже зараз: `players` — список, хід виражений даними (`flip(index)`), колода будується із ЗЕРНА (усім досить одного числа замість розкладки), а приховування невдалої пари — окремий перехід `resolvePeek()`, який може викликати і таймер, і сервер. Мережі й кімнат тут немає — це наступний шар, і він стане поверх, не переписуючи цей | 2026-08-16 |
| Перемикач мови | випадайка з прапорами; поточна мова — прапором у шапці | Кнопка «EN» перемикала дві мови по колу. З чотирма це вже не працює: щоб дістатися четвертої, треба тричі клацнути й тричі перезавантажити сторінку. Пункти лишаються ПОСИЛАННЯМИ — мова живе в адресі, тож у кожної є свій URL, і його має бути видно | 2026-08-16 |
| Додати мову | три кроки, і всі три стереже інваріант | Код у `LANGUAGES`, рядок у таблиці `i18n/languages.ts`, словник у `translations/`. Решта виводиться: матчер, `entries()`, hreflang, sitemap, перелік у перемикачі. `LANGUAGES` — це мови, які сайт СПРАВДІ віддає, а тип `Language` — коди, про які проєкт знає: неповна мова показала б ключі замість тексту, а типи цього не ловлять | 2026-08-16 |
| Розмір дошки у «Знайди пару» | 90% екрана АБО стільки, скільки влазить у висоту — що менше | Сама лише ширина в 90vw на 1920×1080 дала б картку 247px, при 3:4 це 329 у висоту й 1316 на чотири ряди — дошка, яка не вміщається у власний екран. Друга межа рахує ширину, за якої ряди ще влазять: `висота × колонки × 3 / (ряди × 4)`. На високому екрані (1200×1600) виграє 90%, на 16:9 — висота | 2026-08-16 |
| Мовні адреси | `[[lang=lang]]` з матчером; `uk` на голому шляху, `en` за `/en/` | Канон дає два способи (група маршрутів або хук `reroute`) і обирає між ними за кількістю маршрутів: тут їх три, тобто «маршрутів одиниці» — група (SVELTEKIT-DATA § 2.7). Політика адрес — в одному модулі `lib/i18n/routing.ts`, бо її читають матчер, layout, перемикач і генератор sitemap | 2026-08-16 |
| `sitemap.xml` | **генерується** з `build/` після збірки | Рукописний знав лише українські адреси й писав їх без кінцевого слеша при `trailingSlash: 'always'` — тобто кожен рядок був редиректом. Генератор читає canonical і hreflang зі самих згенерованих сторінок, тож розійтися з ними не може | 2026-08-16 |
| Теми | чотири: `dark`, `light-green`, `orange-purple`, `winter` | — | до 2026-08 |
| `light-dark()` (UI-UX § 1.5.1) | **не застосовується** | функція знає рівно дві схеми, а тем тут чотири. Розділ канону сам виводить такий проєкт за свої межі; тема лишається цілком на `[data-theme]` | 2026-08-16 |
| Словник токенів | `--color-*` (`--color-bg-surface`, `--color-text`, `--color-border`, `--color-text-muted`, `--color-warning`) | **не** `--bg-surface` / `--text-primary`, як у CV. Це різні словники, і саме через змішування три елементи інтерфейсу ігнорували тему до 2026-08-16 | 2026-08-16 |
| Sentry | `@sentry/sveltekit` у devDependencies; **DSN не заданий** | `PUBLIC_SENTRY_DSN` порожній і локально, і у workflow, тож SDK не надсилає нічого. Це валідний стан (OBSERVABILITY § 9), а не недогляд: конфіг написаний так, щоб у момент появи DSN нічого не доналаштовувати | 2026-08-16 |
| Source maps | **не публікуються** (`build.sourcemap: false`) | Sentry-плагін вмикав `'hidden'` і мав видалити мапи після вивантаження, але вивантаження не буває без `SENTRY_AUTH_TOKEN` — і весь вихідний код їхав на Pages. Повернути `'hidden'` разом із токеном | 2026-08-16 |
| Аналітика | GA4, **позиція B — без банера згоди** (ANALYTICS § 4.1) | некомерційний освітній проєкт без входу, форм і персональних даних; збирається лише перегляд сторінки. Ризик прийнято свідомо | 2026-08-16 |
| Модель версіонування | **бамп на коміт** (VERSIONING § 1.2) | один автор, деплой на кожен push; номер потрібен, щоб відрізнити свіже від несвіжого, і те, що він є лічильником комітів, приймається свідомо | 2026-08-16 |
| E2E | **немає** | Playwright не стоїть | 2026-08-16 |
| Компонентні тести | `@testing-library/svelte` + jsdom | **вибір за правилом, не відхилення.** CODE-QUALITY § 4.1 обирає середовище за ознакою: браузерний режим — якщо компонент залежить від розмірів, layout, фокуса чи медіазапитів; тут тестуються чисті сервіси й конфіги, тож jsdom і є правильна відповідь | 2026-08-16 |

### Німецька й нідерландська: увімкнені 2026-08-16, вичитки носієм немає

**Цей розділ раніше стверджував протилежне** — що `de` і `nl` у `LANGUAGES`
немає й не буде до появи перевіреного словника. Словники приїхали комітом
`aa08ad3` («Сайт заговорив німецькою й нідерландською»), по 677 ключів на мову,
а розділ лишився. Тобто персональний шар пакета — той самий файл, який агент
читає ДРУГИМ кроком алгоритму README, щоб зрозуміти профіль, — казав про сайт
неправду в тому, скільки в нього мов.

Фактичний стан: `LANGUAGES` віддає всі чотири, обидва словники повні, паритет
ключів тримає `translations.test.ts`, а три кроки увімкнення мови —
`i18n-completeness.test.ts`. Перекладено з англійської, з переведенням одиниць
у метричні; вичитки носієм **не було й немає кому її зробити**.

Звідси відхилення від `I18N-v8` § 3.4 нижче: канон каже тримати такі мови
робочими, але поза індексом, а тут вони в індексі. Рішення лишається як є —
змінити його означає прибрати 36 адрес зі `sitemap.xml` і два `hreflang`, і це
рішення власника сайту, а не наслідок аудиту. Записане, щоб наступний аудит не
відкривав задачу, якої ніхто не ставив.

## Обрані optional-файли пакету

`I18N`, `ANALYTICS`, `DEPENDENCIES`, `VERSIONING`, `DEBUGGING`, `OBSERVABILITY`,
`CLOUD-DATABASE` (клієнт ходить у Realtime Database напряму), `FORM-INPUTS` та
`INPUT-TOOLS` — **застосовні з 2026-08-17**, відколи з'явилися поля вводу.
Не застосовуються: `AUTH-FORM` (немає входу), `AI-PROVIDERS`, `NOTIFICATIONS`
(тости є, але контактного email на сайті немає), `CUSTOM-DOMAIN` (власного
домену немає), `MINIMAP` і `HOLD-SCROLL` — причина нижче. `SCROLLBAR`
застосовується: режим `custom` реалізовано.

### Поля вводу: чого вони вимагають, а чого ні

Доти тут стояло «у проєкті немає жодного поля вводу», і на цьому два файли
пакета випадали з розгляду. Поля є, і їх шість: ім'я гравця й код кімнати
(`OnlineGate`), розмір вольєра (`SizePicker`), масштаб мінікарти
(`ReserveMinimap`), поле dev-панелі та `textarea` запасного шляху копіювання
звіту в чеклисті. Тепер рядок `text-inputs` у «Звірених фактах» стереже саме це.

Що з цього **виконано**: кожне поле має підпис — або обгорткою `<label>`
(`OnlineGate`, `DevPanel`), або `aria-label` (решта). `ACCESSIBILITY-v8` § 4.2
приймає обидві форми, тому `$props.id()` тут не потрібен: пари `for`/`id` немає
жодної. Мінімальна висота 44px на полях `OnlineGate` стоїть явно.

Що **не застосовне**: `PasswordInput` із CapsLock і показом пароля
(`FORM-INPUTS` § 2, § 6) — паролів у проєкті немає; кнопки «вставити /
скопіювати / стерти» (`INPUT-TOOLS` § 3) — жодне поле не заповнюють із буфера.
Лишається розділ про leading-іконку й floating-label, і це смак, а не дефект.

### Смуга прокрутки: власна є, мінімапи й доводчика немає

Прокрутка тут не сторінкова. `html` і `body` заввишки рівно з вікно, а
прокручується `.page-transition-wrapper` усередині — тобто всі смуги в
застосунку внутрішні. Перевірено прямо в браузері: `window.scrollTo({ top:
500 })` лишає `window.scrollY` нулем на кожній сторінці.

**Це і є головне відхилення від канону.** Увесь його код побудований на
прокрутці СТОРІНКИ — `window.scrollY`, `window.scrollTo`,
`documentElement.scrollHeight`. Узятий буквально, він дав би смугу, яка ніколи
не рухається, і виглядав би при цьому бездоганно. Тому прокрутник не мається
на увазі, а публікується явно: `.page-transition-wrapper` реєструє себе через
`use:scrollbar.register`, і доріжка накриває саме його коробку — тобто
починається під шапкою, а не від верху вікна.

Реєстрація знімається з перевіркою «а чи це ще ми»: під час переходу існують
дві обгортки одночасно, і вихідна вмирає ПІСЛЯ появи нової. Без перевірки
смуга зникала б на кожній навігації.

`MINIMAP` і `HOLD-SCROLL` лишаються незастосовними. Мінімапа — для довгих
документів, у яких орієнтуються за формою; тут кожен екран — ігрове поле на
одне вікно. Доводчик (прокрутка від наведення на край) — надбудова, якої ніхто
не просив, і вона додає рух там, де його не чекають.

**Що перевіряти руками** (канон § 11 прямо каже, що машиною це не міряється):
плавність розгортання смуги при наближенні миші, відсутність ривків під час
перетягування й те, що повзунок не відстає від курсора. У прихованій панелі
браузера ці три не перевіряються взагалі: без кадрів пружини не рухаються.

## Свідомі відхилення від канону

| Правило | Стан | Чому |
|---|---|---|
| I18N § 3.1 (HIGH): мова сегментом шляху | **виконано 2026-08-16.** Маршрути живуть під `[[lang=lang]]`, англійська — за `/en/`, українська — на голому шляху | Доти мова була лише в `localStorage`, і наслідків було два: надісланим посиланням не можна було поділитися так, щоб отримувач побачив ту саму мову, а пошуковик бачив один комплект сторінок замість двох |
| I18N § 3.1: `/uk/` теж резолвиться | **відхилення.** `uk` не входить у матчер, тож `/uk/` віддає 404-фолбек | Інакше той самий вміст існував би за двома адресами (`/` і `/uk/`) і конкурував сам із собою в індексі. Канон допускає обидві форми за умови, що canonical вказує на голий шлях; тут обрано просто не мати другої адреси |
| PROJECT-STRUCTURE § 7: розмір файлу | **два перевищення**, обидва в явному списку `OVERSIZED_ALLOWLIST` у `src/structure.test.ts`; список може лише скорочуватися | `game-population/+page.svelte` (998 при межі 400) і `game-mythbusters/+page.svelte` (418). Тут стояло «три перевищення» з числами 1148, 438 і 411, і третім був `GameHeader.svelte` — він вклався в межу й зі списку вийшов, а рядок лишився. Числа беруться зі самого `OVERSIZED_ALLOWLIST`, а не звідси: тест не дає їм зростати й падає на протермінованому винятку. Обидва маршрути вже позбулися логіки — вона в `lib/controllers/`; лишилися розмітка й обробники введення, і різатимуться вони винесенням секцій у `components/` |
| CODE-QUALITY § 6.2: поріг покриття | **налаштований, але не є гейтом** | `vitest.config.ts` оголошує `coverage.thresholds`, проте `npm test` не передає `--coverage`, а провайдера (`@vitest/coverage-v8`) немає в залежностях. Тобто поріг зараз нічого не тримає, і це записано, щоб він не читався як діючий гейт |
| I18N § 3.4 (MEDIUM): машинні переклади поза індексом | **відхилення з 2026-08-16.** `de` і `nl` перекладені з англійської без вичитки носієм, але `INDEXED_LANGUAGES` дорівнює `LANGUAGES`, тобто вони в canonical, hreflang і `sitemap.xml` нарівні з рештою | Канон радить тримати такі мови робочими, але поза індексом, доки їх не вичитають. Тут вичитку нікому зробити ні зараз, ні в осяжному майбутньому, тож «до вичитки» означало б «назавжди», а сховані від пошуку сторінки з повним перекладом користі не дають нікому. Ціна рішення названа: у видачі можуть з'явитися 36 адрес (18 маршрутів × 2 мови) із неперевіреним текстом про те, що для якої тварини отруйне. Прибрати їх з індексу — один рядок у `INDEXED_LANGUAGES`, і це рішення власника сайту, а не наслідок аудиту |
| SECURITY § 9 / DEPENDENCIES § 4.1: `npm audit --omit=dev` | **відхилення в СТРОГІШИЙ бік.** CI виконує `npm audit --audit-level=high` без `--omit=dev` | Канон радить обмежити аудит прод-залежностями, бо саме вони їдуть у браузер. Тут аудит дивиться й на інструментарій: залежностей мало, шуму від цього немає (порівняно: 3 знахідки замість 2), а вразливий `eslint` чи `vite` — це чужий код, який виконується на машині розробника з доступом до всього репозиторію |

## Що не перевіряється автоматично

| Правило | Чому перевірки немає | План |
|---|---|---|
| E2E, axe-аудит, touch targets, FOUC | Playwright не стоїть | ставити разом із першим сценарієм, який без браузера не перевірити |
| Контраст чотирьох тем | статично не перевіряється | перенести `contrast.test.ts` із teatralo4ka |
| ~~Шість попереджень `svelte/no-navigation-without-resolve`~~ | **закрито 2026-08-16.** Правило підняте до `error`. Шість файлів, де навігація йде через `langPath()`, винесені в окремий блок конфіга з причиною: `resolve()` там уже викликається, просто всередині функції, а правило синтаксичне. Справжню помилку — склеювання шляху з `base` вручну — тримає інваріант у `src/structure.test.ts` | — |
| `$props.id()` | звернень нуль, і це правильний стан, але **не з тієї причини, що стояла тут раніше**. Було написано «у проєкті немає жодного `<label>`/`<input>`»; поля з'явилися 2026-08-17 разом зі спільною партією й заповідником, і зараз їх шість. `$props.id()` не потрібен тому, що жодне поле не будує пари `for`/`id`: підпис дає або обгортка `<label>`, або `aria-label`, і `ACCESSIBILITY-v8` § 4.2 приймає обидві форми. Виклики `Math.random()` — це зерно партії й перемішування карток, а не генерація id | — |

## Перевірки, які тут є

| Гейт | Де | Що ловить |
|---|---|---|
| `npm run lint` | CI | eslint; базовий набір за CODE-QUALITY § 6.4.1 увімкнений і перевіряється `eslint-baseline.test.ts` |
| `npm run check` | CI | `svelte-check`, 0 помилок і 0 попереджень |
| `npm test` | CI | 53 файли, 713 перевірок — `npx vitest run`, 2026-08-18. Число супроводжується командою навмисно (AI-AGENT-PITFALLS-v8 § 5.5): тут стояло «20 файлів, 170 перевірок», тобто менше за третину справжнього, і прочитати це можна було лише як «тестів тут небагато» |
| `npm run check:build` | CI, після збірки | інваріанти зібраного HTML: порожнє тіло, `sveltekit-prerender`, canonical, CSP і хеші інлайн-скриптів, `.map` у `build/`, бюджет initial JS, схожі на секрети рядки |
| `git diff --exit-code` | CI, після збірки | збірка не міняє відстежуваних файлів |
| `npm audit --audit-level=high` | CI | вразливості. Поріг `high` свідомий: на `moderate` npm пропонує відкати на кілька мажорів назад. Крок навмисно БЕЗ `--omit=dev`, тобто строгіший за канонічний: дивиться й на інструментарій. Станом на 2026-08-18 — 3 `low` усього, з них 2 у прод-залежностях; перелік і причина нижче |
| `npm run check:rules` | CI, окремий джоб `rules` | **правила доступу до бази** над емулятором: 26 випадків, 9 «застосунок мусить це вміти» й 17 «сторонній не мусить цього могти». Правила «дозволити все» валять другий набір, «заборонити все» — перший, тож зелений результат неможливий випадково. Зворотний експеримент виконано: зняття `!data.exists()` із правила ходів валить прогін із кодом 1 |
| `src/cloud-database.test.ts` | `npm test` | форма коду й форма файлу правил: безумовний дозвіл, дозвіл без `auth`, запис, не звужений до автора, «лише створити» для ходів, підробний час, статичний імпорт SDK, `initializeApp` у тілі модуля, Firebase у `.svelte.ts`, підписка без відписки, версія правил гри |
| `src/dependencies.test.ts` | `npm test` | розкладка залежностей по розділах: кілька lockfile, `latest`/`*`, інструменти в `dependencies`, імпорт неоголошеного пакета і — головне — рантайм-код, що імпортує `devDependency` |
| `src/lib/services/analytics.test.ts` | `npm test` | гарди лічильника: мовчить у dev і під prerender, доходить у production, перевірка плейсхолдера не є мертвою гілкою |
| `src/project-context.test.ts` | `npm test` | «Звірені факти» цього файлу проти самого коду: мови, префікс сховища, порти емулятора, адаптер, наявність полів вводу |

### Знахідки `low`, які лишаються відкритими

`DEPENDENCIES-v8` § 4.1 вимагає перелічувати їх, а не ховати за порогом гейта.
Числа з прогону 2026-08-18.

| Знахідка | Де | Чому не закрита |
|---|---|---|
| `cookie <0.7.0` (GHSA-pxg6-pf52-xh8x) | транзитивно через `@sveltejs/kit`, у прод-залежностях (2 записи) | єдине «виправлення», яке пропонує npm, — `@sveltejs/kit@0.0.30`, тобто відкат на два мажори й непрацездатний проєкт. Сам вектор тут недосяжний: `cookie` розбирає заголовки на СЕРВЕРІ, а профіль static серверного рантайму не має зовсім |
| та сама через `@sveltejs/adapter-static` | інструментарій (1 запис) | той самий ланцюг, лише на етапі збірки |

До 2026-08-18 у цьому переліку було чотири записи, і четвертий приходив від
`@sveltejs/adapter-auto` — пакета, який у проєкті не використовувався взагалі.
Його прибрано.

Окремо варто знати, чому число «у прод-залежностях» щойно виросло з нуля до
двох. Воно виросло не тому, що з'явилася вразливість, а тому, що
`@sentry/sveltekit` переїхав із `devDependencies` у `dependencies` — він
імпортується з `hooks.client.ts`, тобто виконується у браузері відвідувача.
Доти `npm audit --omit=dev` друкував `found 0 vulnerabilities`, і цей нуль
означав не «прод чистий», а «прод описаний неправильно».

## Зображення

Страви намальовані — п'ятнадцять файлів у `static/images/food/`, імена
збігаються з `id` страви. Тварини — 85 фото в `static/images/animals/`.

Спільної заглушки більше немає: вона існувала рівно доти, доки не було страв,
і лишати її означало тримати запасний шлях, яким уже ніхто не піде.

Наявність файлу для КОЖНОЇ страви звіряє інваріант у
`feedingGame.svelte.test.ts`. Це не формальність: `asset()` шляхів не
перевіряє, тож страва без файлу дає порожню картку, яка не падає ніде — ні в
збірці, ні в тестах, — і видно її лише оком, і то якщо ця страва випала в
раунді.

### Континенти й природні зони

Намальовані: сім у `static/images/continents/`, дев'ять у `static/images/biomes/`.
Імена файлів збігаються з ключами `CONTINENTS` і `BIOMES`.

**Каталог `continents` — латиною, і це не дрібниця.** Приїхав він названим із
кириличною «с» (U+0441): на вигляд той самий рядок, а шлях, написаний у коді
латиною, давав би 404 на GitHub Pages. Ані `svelte-check`, ані eslint, ані
збірка цього не бачать, і локальний сервер віддає файл нормально — бо шукає за
тим самим байтовим ім'ям. Стереже інваріант «в іменах у `static/` немає
не-ASCII» у `src/structure.test.ts`.

Смітник — іконка lucide, зображення не потребує.

## Локальні пастки

| Пастка | Що саме |
|---|---|
| Панель браузера прихована — анімації НЕ йдуть | `requestAnimationFrame` не спрацьовує (0 кадрів за 500 мс), тож переходи Svelte замерзають на першому кадрі: вихідні вузли лишаються в DOM, і підрахунок елементів за селектором показує дублікати. `setTimeout(60)` там триває ~770 мс, тобто вікно подвійного кліку в 400 мс встигає закритися. Обидва ефекти дають «падіння» на цілком робочому коді — перевіряти треба СТАН контролера, а не кількість вузлів |
| `resolve()` НЕ додає кінцевого слеша | а в проєкті `trailingSlash: 'always'`. Без ручної нормалізації кожне внутрішнє посилання стає редиректом і розходиться з canonical. Нормалізація — в `langPath()`; юніт-тест сам по собі цього не ловив, бо слеш додавав мок `$app/paths` |
| Текст `<style>` у коментарі всередині `script` ламає `svelte-check` | компілятор такий файл збирає без слова, а `svelte-check` каже «`script` was left open» на весь файл. Назви тегів у коментарях пишуться без кутових дужок |
| `vite preview` не бачить файлів, створених ПІСЛЯ його старту | Сервер будує список файлів на старті. Після `npm run build` при живому прев'ю сторінка віддає 404 на нові хеші бандла, і це виглядає як зламана збірка. Прев'ю треба **перезапускати** після кожної збірки (AI-AGENT-PITFALLS § 5.8) |
| Хеш інлайн-скрипта в CSP рахується з **нормалізованим** текстом | Парсер HTML зводить `\r\n` до `\n` ще до того, як CSP хешує тіло скрипта. На Windows-чекауті з `core.autocrlf` хеш «як є» не збігається НІКОЛИ: браузер відхиляє анти-FOUC скрипт, тема не застосовується на першому кадрі, і жодна перевірка по джерелах цього не бачить. `svelte.config.js` нормалізує; `check-build.mjs` звіряє |
| `page.url.searchParams` під час prerender **кидає** | «Cannot access url.searchParams on a page with prerendering enabled» валить збірку цілком. Будь-яке читання query-параметрів guard-иться `browser` — приклад у DEBUGGING § 2.1 написаний без цього guard, бо він для іншого профілю |
| Порти — **5192** (dev) і **5191** (перегляд збірки) | конфігурації `vetcrew-dev` і `vetcrew-preview` у `.claude/launch.json` кореневої теки `GitHub`, обидві зі `--strictPort`. Доти запису не було зовсім, і `vite dev` ішов на типовий 5173 — спільний з усіма сусідами, тож можна було дивитись чужий сайт і не помітити |
| `firebase-tools` **не** в `devDependencies` | його транзитивна залежність `superstatic` оголошує `node: "20 \|\| 22 \|\| 24"`, а в `.npmrc` стоїть `engine-strict=true`. Разом на Node 25 це валить `npm install` **цілком**, а не лише firebase-tools. Тому CLI приїжджає через `scripts/firebase-cli.mjs`, який знімає перевірку рухомого складу рівно для цього виклику. Не переносити пакет у залежності «щоб було правильніше» — зламається щоденна робота |
| Емулятор лишає java-процес на **9010** після падіння | наступний прогін падає з `Could not start Database Emulator, port taken`, і це виглядає як зламані правила. Знімати: `netstat -ano \| grep 9010` → `taskkill /PID <pid> /F` |
| Межа очікування ходу перевіряється **серверним** часом | `at` у ході й `startedAt` у кімнаті ставить сервер, і правило бази вимагає, щоб позначка лягла у вікно навколо `now`. Замінити на `Date.now()` на клієнті — і будь-який гравець оголошує чужий хід простроченим коли завгодно, забираючи чергу |
| CSP перевіряється лише на **зібраному** сайті | у dev політика приходить заголовком, у пререндері — мета-тегом, і поведінка різна. Дивитись треба `npm run preview`, а не `npm run dev` |
| `base` різний у dev і production | `svelte.config.js`: `NODE_ENV === 'production' ? '/VetCrewGames' : ''`. Тобто локально застосунок за коренем, а на Pages — за префіксом. Шлях, зашитий вручну, зламається на одному з двох |
| Playwright тут **не стоїть** | `playwright.config.*` у проєкті немає. Не створюй файлів під нього — вони не запустяться, і ніхто про це не повідомить (AI-AGENT-PITFALLS § 1.3) |
| Мета-тег `color-scheme` рухомий | він оновлюється динамічно під тему, щоб заблокувати мобільний Force Dark Mode. Статичне значення поверне баг |

## Історія рішень, які легко скасувати помилково

- **2026-08-16, `--color-warning` доданий у чотири теми.** Його не було, хоча
  `--color-success` і `--color-error` були, і `RoundIndicator` тримав жовтий
  літералом у фолбеку. У двох світлих темах значення темніше (`#b8860b`), бо
  `#facc15` на світлому тлі не читається.
- **2026-08-16, фолбеки в `var()` прибрані.** `var(--bg-surface, #333)` виглядав
  як страховка, а насправді БУВ значенням: змінної з таким іменем у проєкті
  немає. Фолбек у `var()` для змінної, якої не оголошує жодна тема, — це спосіб
  сховати помилку, а не захиститися від неї (UI-UX § 4).
- **2026-08-16, `unsafe-inline` прибраний зі `script-src`.** Доки він там був,
  SvelteKit **не додавав хешів** власному bootstrap-скрипту — він бачив, що
  інлайн і так дозволений. Тобто політика не покривала жодного скрипта на
  сторінці. Разом із цим анти-FOUC скрипт переїхав **нижче** `%sveltekit.head%`:
  мета-політика не діє на те, що стоїть вище за неї.
- **2026-08-16, збереження налаштувань — наскрізним записом, без `$effect`.**
  `$effect.root` у module-level синглтоні давав ефекти, яких ніхто не прибирає,
  і зберігав те, що щойно прочитав зі сховища: кожне відкриття сайту
  перезаписувало чотири ключі. Підписка на системну тему живе в `init()`, чий
  cleanup повертає `+layout.svelte`.
- **2026-08-16, `aria-label` НЕ проходить через `formatPlain`.** Ця функція
  міняє кириличну «і» на латинську «i» заради шрифту inglobal: правильно для
  того, що малюється, і неправильно для того, що читає машина. Той самий
  висновок уже був записаний для `<title>`.

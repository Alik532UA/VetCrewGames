/**
 * Числа економіки заповідника.
 *
 * Усі — з технічного завдання, і всі в одному місці навмисно: гра НАВЧАЄ саме
 * цією платіжною матрицею, тож підкрутити її має бути можна, не шукаючи цифри
 * по коду.
 */

/**
 * Скільки логічних тіків триває ігровий день.
 *
 * Тік — 100мс реального часу при швидкості x1, отже день — 30 секунд. Число
 * тут, а не в компоненті, бо від нього залежить і умова програшу (30 днів), і
 * щоденні витрати. Швидкості x2/x5 його не змінюють: вони множать КІЛЬКІСТЬ
 * тіків за кадр, а не тривалість дня.
 */
export const TICKS_PER_DAY = 300;

/** Скільки днів поспіль у мінусі за «Користю планеті» означають кінець гри. */
export const COLLAPSE_DAYS = 30;

/** Звідки береться тварина. Від цього залежить усе інше в її долі. */
export type AnimalOrigin = 'official' | 'black-market' | 'rescue';

export interface OriginTerms {
	/** Ціна самої тварини. */
	price: number;
	/** Логістика чи транспортування — платиться завжди, навіть за порятунок. */
	logistics: number;
	/** Одразу до «Користі планеті». Порятунок дає нуль — див. докблок нижче. */
	impact: number;
	/** Одразу до репутації: публіка бачить вчинок, а не його наслідки. */
	reputation: number;
	/**
	 * Шанс, що тварину колись удасться випустити. Народжена в неволі майже
	 * ніколи не повернеться в природу — і саме це робить дешеву покупку
	 * дорогою: випуск дає +150, а купленій він недоступний.
	 */
	releaseChance: number;
}

/**
 * Порятунок дає НУЛЬ до «Користі планеті» — і це найгостріше місце всієї
 * економіки.
 *
 * З одного боку, у природи забрали особину: у когось відняли здобич, ланка
 * випала. З іншого — зʼявився шанс урятувати. Сума чесно дорівнює нулю: користь
 * настане тоді, коли тварина ПОВЕРНЕТЬСЯ, а не коли її привезли.
 *
 * Суспільство ж реагує однозначно: гуманний вчинок видно одразу, тож репутація
 * росте. Саме цей розрив між «робити добро» і «виглядати добре» і виправдовує
 * дві шкали замість одного числа під двома назвами.
 *
 * Покупки лишаються в мінусі: вони створюють ПОПИТ, а попит вимиває тварин із
 * природи далі, незалежно від того, як добре їм буде тут.
 */
export const ORIGINS: Record<AnimalOrigin, OriginTerms> = {
	official: { price: 10_000, logistics: 1_000, impact: -5, reputation: 0, releaseChance: 0.05 },
	'black-market': {
		price: 5_000,
		logistics: 2_000,
		impact: -25,
		reputation: -12,
		releaseChance: 0
	},
	rescue: { price: 0, logistics: 3_000, impact: 0, reputation: 2, releaseChance: 0.9 }
};

/**
 * Шлях тварини в числах: привезли → вилікували → відпустили.
 *
 * Вилікувана тварина в неволі допомагає природі мало — звідси +1. Публіці ж
 * видно саме одужання, тому +5. А повернення в природу — це те, заради чого все
 * затівалося: +50, тобто вп’ятдесят разів більше за лікування.
 */
export const HEAL_IMPACT = 1;
export const HEAL_REPUTATION = 5;
export const RELEASE_IMPACT = 50;

/**
 * Будівництво САМЕ ПО СОБІ природі не допомагає: витрачені ресурси, зайнята
 * земля, жодної врятованої тварини. Мінус невеликий, але він робить старт
 * від’ємним — і змушує дійти до першого випуску, а не будувати нескінченно.
 */
export const ENCLOSURE_IMPACT = -2;

/**
 * Радіус ділянки заповідника у світових одиницях.
 *
 * Це МЕЖА, а не декорація: за нею будувати не можна, і на карті вона позначена
 * пунктиром. Земля при цьому тягнеться далеко за неї — карта не обривається, бо
 * природа не закінчується там, де закінчується твоя ділянка.
 */
export const RESERVE_RADIUS = 14;

/** Скільки «Користі планеті» треба набрати, щоб виграти партію. */
export const IMPACT_TO_WIN = 10_000;

/**
 * Репутація — друга шкала, і вона керує ГРІШМИ.
 *
 * «Користь планеті» лишається моральним підсумком і єдиною умовою програшу;
 * репутація вирішує, скільки жертвують. Розділені вони не заради складності, а
 * тому що розходяться в конкретних місцях: узяв хвору тварину без ветеринара —
 * репутація впала, а планеті ти таки допоміг.
 *
 * Випуск підіймає ОБИДВІ. Інакше найбільша нагорода гри стала б суто
 * оборонною: +150 у шкалу, яка лише боронить від програшу, і жодної копійки.
 */
export const REPUTATION_MIN = 0;
export const REPUTATION_MAX = 100;
export const STARTING_REPUTATION = 50;

/** Скільки репутації додає випуск у природу. */
export const RELEASE_REPUTATION = 10;

/**
 * Публіка ЗАБУВАЄ. Репутація спадає щодня сама собою.
 *
 * Без спаду шкала 0–100 насичується за десять хвилин: три тварини — і ти
 * назавжди на сотні, після чого репутація перестає бути рішенням і стає
 * туторіалом. Зі спадом її УТРИМАННЯ і є грою: заповідник, який нічого не
 * робить, поступово зникає з поля зору, хоч би скільки добра зробив торік.
 */
export const REPUTATION_DECAY_PER_DAY = 0.5;

/**
 * Кампанія в соцмережах: гроші в репутацію, раз на день.
 *
 * Ціна й межа обовʼязкові. Безкоштовний плюс натискали б нескінченно, і
 * репутація перетворилася б на кнопку «дати грошей». А раз на день — тому що це
 * інформаційний привід, а не кран: другий допис того самого дня нікого не
 * переконує.
 */
export const CAMPAIGN_PRICE = 2_000;
export const CAMPAIGN_REPUTATION = 3;

/**
 * Скільки забирає прийом хворої тварини, коли в заповіднику немає ветеринара.
 *
 * Гра дозволяє це зробити — забрати тварину з біди краще, ніж лишити її там.
 * Але взяти й не мати кому лікувати — це те, за що фонд критикують, і саме
 * тому число тут, а не заборона.
 */
export const NO_VET_REPUTATION = -3;

/** Скільки коштує день утримання однієї тварини. */
export const UPKEEP_PER_ANIMAL = 60;

/**
 * Ціна вольєра росте КВАДРАТИЧНО з розміром: `500 × розмір²`.
 *
 * Лінійна ціна зробила б вибір розміру фальшивим — накопичив грошей, збудував
 * десятку, і жодного рішення більше немає. Квадрат тримає великі вольєри
 * рідкісними: одиничка коштує 500, четвірка (рекомендована леву) — 8 000, а
 * десятка — 50 000, тобто цілий стартовий бюджет.
 */
export const ENCLOSURE_PRICE_FACTOR = 500;

/**
 * Якість вольєра — другий вимір поруч із розміром.
 *
 * Розмір вирішує, ХТО тут поміститься; якість — наскільки добре йому тут.
 * Тому дешевий великий вольєр і дорогий тісний — це два різні рішення, а не
 * одне й те саме число під іншою назвою.
 *
 * Середня якість базова (множник 1). Дешева гальмує, дорога пришвидшує — і
 * коштує втричі, тож на початку партії вона недосяжна для великих розмірів.
 */
export const QUALITIES = [1, 2, 3] as const;
export type Quality = (typeof QUALITIES)[number];

export const QUALITY_PRICE: Record<Quality, number> = { 1: 1, 2: 1.8, 3: 3 };
export const QUALITY_SPEED: Record<Quality, number> = { 1: 0.7, 2: 1, 3: 1.4 };

export const enclosurePrice = (size: number, quality: Quality = 2) =>
	Math.round(ENCLOSURE_PRICE_FACTOR * size * size * QUALITY_PRICE[quality]);

/**
 * Міцність: 1 — щойно збудований, 0 — руїна. Спадає щодня.
 *
 * 0.02 на день означає пʼятдесят днів від нового до руїни — довше за умову
 * програшу, тож ремонт стає потрібним не одразу, а тоді, коли гравець уже
 * навчився решти.
 */
export const WEAR_PER_DAY = 0.02;

/**
 * Пороги, на яких зношений вольєр ПАДАЄ в якості.
 *
 * Не плавний множник, а сходинки — навмисно: гравець має бачити, що вольєр
 * «став гіршим», а не здогадуватися, чому числа поповзли. Нижче 0.6 якість
 * падає на щабель, нижче 0.3 — на два.
 */
export const WEAR_ONE_STEP = 0.6;
export const WEAR_TWO_STEPS = 0.3;

/** Повний ремонт коштує чверть ціни вольєра; частковий — пропорційно зносу. */
export const REPAIR_PRICE_SHARE = 0.25;
export const repairPrice = (size: number, quality: Quality, durability: number) =>
	Math.round(enclosurePrice(size, quality) * REPAIR_PRICE_SHARE * (1 - durability));

/** Підняти якість коштує різницю в ціні плюс надбавку за перебудову. */
export const UPGRADE_SURCHARGE = 1.2;
export const upgradePrice = (size: number, from: Quality, to: Quality) =>
	Math.round((enclosurePrice(size, to) - enclosurePrice(size, from)) * UPGRADE_SURCHARGE);

/**
 * Щоденне утримання вольєра — за розмір, а не за штуку.
 *
 * Без цього великий вольєр був би СТРОГО кращим: разова ціна швидко
 * окупається, і в пізній грі всі будували б десятки. Щоденна плата лишає вибір
 * живим до кінця партії.
 */
export const UPKEEP_PER_SIZE = 15;

/** Денна платня. Ветеринар лікує, доглядач знімає стрес. */
export const WAGES = { vet: 120, keeper: 80 } as const;
export type StaffRole = keyof typeof WAGES;

/**
 * Скільки одужання додає один ветеринар за день, у частках від повного.
 *
 * Один ветеринар витягує одну тварину приблизно за десять днів; двоє на одну —
 * удвічі швидше. Розподіл між тваринами рівний: черги в MVP немає.
 */
export const RECOVERY_PER_VET_DAY = 0.1;

/**
 * Стрес росте щодня, поки в тварини немає доглядача, і сповільнює одужання.
 *
 * Один доглядач заспокоює трьох тварин. Понад це стрес росте, і при повному
 * випуск блокується — тварина, яка боїться людей і не вміє добувати їжу, у
 * природі не виживе.
 */
export const ANIMALS_PER_KEEPER = 3;
export const STRESS_PER_DAY = 0.08;
export const STRESS_RELIEF_PER_DAY = 0.12;

/** Вище за це випуск неможливий, хоч би яким здоровим було тіло. */
export const STRESS_BLOCKS_RELEASE = 0.7;

/** Скільки грошей на день приносить кожен пункт репутації. */
export const DONATION_PER_REPUTATION = 4;

/** З чого починається партія. */
export const STARTING_BUDGET = 50_000;

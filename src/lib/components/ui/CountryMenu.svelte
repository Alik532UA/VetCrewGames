<script lang="ts">
	import { untrack } from 'svelte';
	import { t, formatFont } from '$lib/i18n';
	import { loadAccountText } from '$lib/i18n/account';
	import { countriesByRegion, filterRegions, type RegionGroup } from '$lib/config/regions';
	import { settings } from '$lib/services/settings.svelte';
	import { columnNeighbourIn, fitMenu, rowNeighbourIn } from '$lib/utils/menuColumns';
	import CountryOption from './CountryOption.svelte';

	/**
	 * ПАНЕЛЬ вибору країни: поле пошуку й 262 пункти, розбиті на регіони.
	 *
	 * ## Чому окремий файл від `CountryPicker.svelte`
	 *
	 * Шов тут не вигаданий під межу розміру, хоч вона його й виявила (391 SLOC
	 * при межі 300 у `structure.test.ts` § 7): кнопка знає, ЩО вибрано, а панель
	 * — ЯК вибирають. Розділені, вони не мають спільного стану взагалі: панель
	 * отримує поточне значення, віддає нове й гасне. Усе, що стосується
	 * клавіатури, прокрутки й пошуку, живе тут і не існує, поки панель закрита.
	 *
	 * ## Коробку задає БАТЬКО
	 *
	 * `position: absolute` тут, а `position: relative` — на `.country` у
	 * `CountryPicker.svelte`. Це єдина зв'язка між файлами, і вона названа: без
	 * неї панель відносилася б до першого позиційованого предка вище, тобто
	 * поїхала б у кут панелі-господаря.
	 *
	 * ## КОЛОНКИ, а не один довгий стовпчик
	 *
	 * Скарга автора зі знімками: «актуальний результат — довгий список; очікуваний
	 * — кілька колонок як у CV». Було видно вісім країн із 262 на екрані будь-якої
	 * ширини. Тепер розкладка — CSS multicol, і три рішення, з яких вона
	 * складається, лежать поруч зі своїми правилами в стилях нижче: ширина
	 * колонки замість їхньої кількості (`.menu__list`), нерозрізаний розділ
	 * (`.menu__group`) і розмір панелі із заміру вільного місця (`.menu`, а сам
	 * замір — у `utils/menuColumns.ts`). Клавіатура на це відповіла двома
	 * стрілками вбік — див. `stepColumn` і `atEdge`.
	 */
	interface Props {
		/** Обраний код. Порожній рядок — «без прапора». Лише читається. */
		value: string;
		/** Основа `data-testid`, та сама, що в кнопки. */
		scope: string;
		/**
		 * Із чого починається пошук.
		 *
		 * Не завжди порожній: літера, натиснута на закритій кнопці, відкриває
		 * панель уже з нею — це те, що нативний `select` робив сам.
		 */
		seed?: string;
		/**
		 * КНОПКА, ПІД ЯКОЮ СТОЯТИ. Без неї панель не знає свого місця.
		 *
		 * Панель живе в `<body>`, а не в коробці кнопки — інакше її накриває
		 * сусідня картка, і жоден `z-index` цьому не допомагає (причина повністю
		 * названа у `utils/menuColumns.ts`). Ціна переїзду — координати: їх ставить
		 * `fitMenu` із заміру цієї кнопки.
		 */
		anchor: HTMLElement | null;
		onpick: (code: string) => void;
		onclose: () => void;
	}

	let { value, scope, seed = '', anchor, onpick, onclose }: Props = $props();

	/**
	 * РЯДКИ ПАНЕЛІ — З ЛІНИВОГО ЧАНКА, і словник тримається тут, а не приходить
	 * пропом.
	 *
	 * Чому чанк: головний словник стоїть рівно на межі бюджету (120 КБ gzip зі
	 * стелі 110), тож дев'ять нових ключів × чотири мови впали б на
	 * `npm run check:build`. Ключі живуть у `i18n/account/`, і `t()` про них не
	 * знає в принципі — він типізований по головному словнику.
	 *
	 * Чому НЕ проп, як у `auth/AuthForm.svelte`. Там перекладач передає сторінка,
	 * бо вона його вже має. Тут інакше: цей примітив стоїть на ДВОХ незв'язаних
	 * сторінках, і пропом його довелося б протягувати через `CountryPicker` та
	 * `OnlineGate` — тобто сторінка «Знайди пару» мусила б завантажувати словник
	 * акаунта заздалегідь і знати, що він їй для цього потрібен.
	 *
	 * ЗАСТЕРЕЖЕННЯ З ТОГО Ж ДОКБЛОКА ВИКОНАНО: у стані лежить СЛОВНИК, а
	 * перекладач похідний. Функція в `$state` не перемальовує — це вже коштувало
	 * екрана з ключами замість тексту.
	 */
	let dict = $state<Record<string, string>>({});
	const text = $derived((key: string) => dict[key] ?? key);

	$effect(() => {
		const wanted = settings.locale;
		void loadAccountText(wanted).then((loaded) => {
			// Мову могли перемкнути, поки чанк їхав: без перевірки пізніший запит
			// перетер би свіжіший результат.
			if (settings.locale === wanted) dict = loaded;
		});
	});

	/**
	 * ПОЧАТКОВІ ЗНАЧЕННЯ БЕРУТЬСЯ З ПРОПІВ РІВНО РАЗ — через `untrack`.
	 *
	 * Прямий `$state(seed)` компілятор не пускає (`state_referenced_locally`), і
	 * має рацію: так читається лише перше значення, а виглядає це як звʼязок.
	 * Тут одноразовість — саме те, що потрібно: панель існує від відкриття до
	 * закриття (`{#if open}` у `CountryPicker.svelte`), тобто кожне відкриття —
	 * новий екземпляр із новим початковим станом. `untrack` каже це вголос.
	 */
	let query = $state(untrack(() => seed));
	/**
	 * Куди клавіатуру поставили ОСТАННІМ натиском. Не те саме, що `active`.
	 *
	 * Порожній рядок означає пункт «без прапора» — те саме значення, що й у
	 * `value`.
	 */
	let cursor = $state(untrack(() => value));
	let panel = $state<HTMLElement | null>(null);
	let search = $state<HTMLInputElement | null>(null);

	/*
	 * Групи рахуються на вимогу мови, а не тримаються готовими: сортування назв
	 * колатором залежить від мови, а мову перемикають на цій самій сторінці.
	 * Заміряно: побудова всього переліку — 0,1–0,7 мс, тож кешувати нічого.
	 */
	const groups = $derived<RegionGroup[]>(countriesByRegion(settings.locale, t, text));
	const shown = $derived(filterRegions(groups, query, settings.locale));

	/**
	 * «Без прапора» видно, лише поки нічого не набрано.
	 *
	 * Це не країна, а свідома відповідь «не показувати», і під запит вона не
	 * підходить ніяк: рядок, який лишається на місці, що б ти не набрав,
	 * читається як збій фільтра. Заразом це дає «нічого не знайдено» право
	 * з'явитися — інакше в панелі завжди був би хоч один пункт.
	 */
	const withNone = $derived(query.trim() === '');

	/**
	 * Чи малювати лише прапори. Та сама умова, що у `withNone`.
	 *
	 * Умова саме на ПОРОЖНІЙ запит, а не на «мало знахідок»: перехід між двома
	 * виглядами мусить залежати від дії людини, а не від того, скільки країн
	 * підійшло, — інакше сітка перебудовувалася б сама собою під час набору.
	 *
	 * ОДНАКОВО НА ВСІХ СТОРІНКАХ, а не лише в компактному режимі. Доти цей вигляд
	 * залежав від `compact`, тобто від того, чи кнопка — сам прапор; на акаунті
	 * панель через це малювала 262 назви, і скарга автора була саме про це: «тільки
	 * прапори, а при пошуку можуть і назви». Питання, на яке відповідає ця панель,
	 * від форми кнопки не залежить: людина шукає СВІЙ прапор, і назви поруч роблять
	 * сітку довгим списком, у якому прапор доводиться вичитувати.
	 *
	 * Доступність від цього не страждає й не може: назва країни стоїть в
	 * `aria-label` пункту ЗАВЖДИ, тобто для скрінрідера список не міняється
	 * зовсім — міняється лише те, що намальовано.
	 */
	const flagsOnly = $derived(query.trim() === '');

	/** Порядок, у якому по пунктах ходять стрілки. Той самий, що на екрані. */
	const order = $derived([
		...(withNone ? [''] : []),
		...shown.flatMap((group) => group.countries.map((country) => country.code))
	]);

	/**
	 * АКТИВНИЙ ПУНКТ ЗАВЖДИ ІСНУЄ, поки в списку є хоч один рядок.
	 *
	 * Це не дрібниця, а ВИПРАВЛЕНИЙ ДЕФЕКТ, знайдений заміром клавіатури:
	 * `cursor` починався з поточної країни, а після набору фільтр її викидав —
	 * тобто `aria-activedescendant` ставав нічим, і `Enter` після набору не
	 * вибирав НІЧОГО. Треба було спершу натиснути стрілку.
	 *
	 * Тепер клавіші пишуть у `cursor`, а на екран іде `active`: `cursor`, якщо він
	 * ще в списку, інакше перший рядок. Тобто набрав «нім» → Enter → Німеччина.
	 */
	const active = $derived(order.includes(cursor) ? cursor : (order[0] ?? ''));

	const optionId = (code: string) => `${scope}-opt-${code === '' ? 'none' : code}`;

	/**
	 * Пункт, на який стрілка ВНИЗ або ВГОРУ перейде з поточного. Закільцьовано.
	 *
	 * ЛІНІЙНИЙ КРОК ТУТ І Є «У МЕЖАХ КОЛОНКИ», і це властивість CSS multicol, а
	 * не збіг: колонки заповнюються в порядку документа — спершу вся перша, потім
	 * друга. Тобто наступний пункт у `order` завжди лежить під поточним, а на межі
	 * колонки — у голові наступної.
	 *
	 * ЗАМІРЯНО НАТИСКАННЯМ, а не виведено: 262 кроки вниз при вікні 1280×720
	 * (чотири колонки на 4, 192, 379 і 567). `left` змінився РІВНО ТРИЧІ — на
	 * кроках 59, 110 і 172, тобто на межах Європа→Азія, Азія→Африка,
	 * Африка→Північна Америка; між ними росла тільки висота. Це той самий прогін,
	 * що тепер лежить у `tests/country-menu.spec.ts`.
	 *
	 * Саме тому перехід на колонки нічого тут не зламав, хоч задача цього й
	 * очікувала: ламається лінійний крок у СІТЦІ, де порядок документа йде
	 * рядками, а не в колонках, де він іде стовпцями. Сітка з'явилася —
	 * `flagsOnly`, — і там «вниз» веде `stepRow`, а не цей крок.
	 */
	function step(by: number) {
		if (order.length === 0) return;
		const at = order.indexOf(active);
		cursor = order[(at + by + order.length) % order.length] ?? order[0];
	}

	/**
	 * Пункт у СУСІДНІЙ КОЛОНЦІ на тій самій висоті — стрілка вбік.
	 *
	 * Номер колонки не рахується арифметикою: скільки пунктів увійшло в колонку,
	 * вирішує браузер. Тому питаємо координати — і сам пошук, разом із причиною,
	 * живе в `utils/menuColumns.ts`.
	 */
	function stepColumn(by: number) {
		const at = order.indexOf(active);
		cursor = order[columnNeighbourIn(panel, order.length, at, by)] ?? cursor;
	}

	/** Пункт у сусідньому РЯДКУ — стрілка вниз/вгору в сітці прапорів. */
	function stepRow(by: number) {
		const at = order.indexOf(active);
		cursor = order[rowNeighbourIn(panel, order.length, at, by)] ?? cursor;
	}

	/**
	 * Каретка стоїть на краю набраного, тобто стрілці вбік нікуди її вести.
	 *
	 * СТРІЛКИ ВБІК НЕ ЗАБИРАЮТЬСЯ В ПОЛЯ ПОШУКУ, поки в ньому є що правити. Це
	 * поле — текстове, і «вліво» в ньому означає «на символ назад»; перехопити це
	 * заради колонок означало б, що набране «німеччнина» неможливо виправити, не
	 * стерши до помилки. Тому вбік ходить лише та стрілка, яка в полі однаково
	 * нічого не зробила б: каретка вже на початку (вліво) або в кінці (вправо), і
	 * нічого не виділено. За порожнього запиту обидві умови справджуються самі —
	 * тобто в режимі перегляду колонки доступні завжди.
	 */
	function atEdge(side: number) {
		if (!search) return true;
		const at = search.selectionStart;
		return at !== null && at === search.selectionEnd && at === (side > 0 ? query.length : 0);
	}

	/**
	 * Клавіатура на ПОЛІ ПОШУКУ — воно ж і є контролом, поки панель відкрита.
	 *
	 * `Tab` і `Shift+Tab` затримуються навмисно: поки панель відкрита, фокус із
	 * неї не виходить. Ціна названа: вихід лишається один — `Escape`, і саме
	 * тому він оголошений на полі через `aria-keyshortcuts`.
	 */
	function onKeydown(event: KeyboardEvent) {
		switch (event.key) {
			case 'ArrowDown':
			case 'ArrowUp': {
				event.preventDefault();
				const down = event.key === 'ArrowDown' ? 1 : -1;
				// У сітці прапорів «вниз» — це рядок нижче, а не наступний пункт:
				// наступний лежить праворуч (див. докблок `step`).
				if (flagsOnly) stepRow(down);
				else step(down);
				break;
			}
			case 'ArrowRight':
			case 'ArrowLeft': {
				const aside = event.key === 'ArrowRight' ? 1 : -1;
				if (!atEdge(aside)) break;
				event.preventDefault();
				stepColumn(aside);
				break;
			}
			case 'Home':
				event.preventDefault();
				cursor = order[0] ?? '';
				break;
			case 'End':
				event.preventDefault();
				cursor = order.at(-1) ?? '';
				break;
			case 'Enter':
				event.preventDefault();
				if (order.includes(active)) onpick(active);
				break;
			case 'Escape':
				event.preventDefault();
				// Без цього Escape закрив би заразом і те, що стоїть вище в дереві.
				event.stopPropagation();
				onclose();
				break;
			case 'Tab':
				event.preventDefault();
				break;
		}
	}

	/**
	 * РОЗМІР ПАНЕЛІ — З ЗАМІРУ ВІЛЬНОГО МІСЦЯ. Що саме міряється й чому цього не
	 * зробити в CSS — у `fitMenu`; звідти ж береться кількість колонок, бо вона
	 * похідна від ширини.
	 *
	 * Ефект, а не `{@attach}`: обидва спрацьовують після монтування й обидва
	 * прибирають за собою поверненою функцією, але панель уже має `bind:this` —
	 * тобто вузол тут є, а рядок у тезі кнопки лишається один.
	 */
	$effect(() => (panel ? fitMenu(panel, anchor) : undefined));

	/**
	 * Активний пункт видно, а не «десь у прокрутці».
	 *
	 * `block: 'nearest'` — щоб крок стрілкою не смикав список на середину:
	 * прокрутка рухається рівно настільки, щоб пункт з'явився.
	 */
	$effect(() => {
		panel?.querySelector(`#${CSS.escape(optionId(active))}`)?.scrollIntoView({ block: 'nearest' });
	});

	/** Фокус заходить у поле пошуку, щойно панель з'явилася: стрілкам треба звідки почати. */
	function focusSearch(node: HTMLInputElement) {
		node.focus();
	}

	/**
	 * ФОКУС НЕ ВИХОДИТЬ ІЗ ПОЛЯ ПОШУКУ, ЩО Б У ПАНЕЛІ НЕ НАТИСНУЛИ.
	 *
	 * Один обробник на всю панель замість 262 на кожній кнопці, і він потрібен
	 * саме тому, що натискають не лише по пунктах: клік по заголовку регіону або
	 * по відступу панелі теж знімає фокус із поля — Chrome при `mousedown` на
	 * нефокусований елемент віддає фокус `body`. Після цього `Escape` і стрілки
	 * не діяли б, тобто панель, відкрита мишею, лишалася б без клавіатури.
	 *
	 * Поле пошуку — єдиний виняток: у ньому `mousedown` ставить каретку.
	 *
	 * `preventDefault` на `mousedown` НЕ скасовує наступний `click`, тож вибір
	 * пункту мишею працює як звичайно.
	 */
	function keepFocus(event: MouseEvent) {
		const target = event.target as Node | null;
		if (search && target && (target === search || search.contains(target))) return;
		event.preventDefault();
	}
</script>

<!--
	`onmousedown` тут не «клік по панелі», а ТРИМАЧ ФОКУСА — див. `keepFocus`.

	Тому й глушимо правило: воно вимагає ролі від елемента з обробником мишки, а
	роль тут була б неправдою — панель нічого не робить у відповідь на натиск,
	вона його СКАСОВУЄ. Клавіатурного дубля цьому обробнику не треба й не може
	бути: з клавіатури фокус із поля не виходить зовсім, бо `Tab` затримано.
-->
<!-- svelte-ignore a11y_no_static_element_interactions -->
<div class="menu" bind:this={panel} onmousedown={keepFocus} data-testid="{scope}-menu">
	<input
		type="text"
		class="menu__search"
		bind:this={search}
		bind:value={query}
		onkeydown={onKeydown}
		role="combobox"
		aria-expanded="true"
		aria-controls="{scope}-list"
		aria-activedescendant={order.includes(active) ? optionId(active) : undefined}
		aria-autocomplete="list"
		aria-label={text('account.countrySearch')}
		aria-keyshortcuts="Escape"
		placeholder={text('account.countrySearch')}
		autocomplete="off"
		spellcheck="false"
		data-testid="{scope}-search-input"
		{@attach focusSearch}
	/>

	<!--
		ПРОКРУТКА — НА ОБГОРТЦІ, а колонки — на самому списку, і поміняти їх місцями
		не можна.

		CSS multicol із ВИЗНАЧЕНОЮ висотою не прокручується вниз — він додає
		колонки вбік. Тобто якби `max-height` і `overflow-y` лишилися на тому самому
		елементі, що й `columns`, 262 країни поїхали б у тридцять колонок
		горизонтальної прокрутки. Тому висоту тримає обгортка, а список під нею має
		висоту `auto` — і саме тому браузер сам вирішує, де рвати колонки.
		Та сама будова в `CV` (`HeaderSection.svelte`), і причина там названа тим
		самим словом.

		Обгортка БЕЗ РОЛІ й поза `listbox`, а не всередині: у `role="listbox"` за
		ARIA не буває нічого, крім `option` і `group`, — те саме правило, через яке
		«нічого не знайдено» лежить нижче, а не порожнім рядком у списку.
	-->
	<div class="menu__scroll">
		<div
			class="menu__list"
			class:menu__list--flags={flagsOnly}
			id="{scope}-list"
			role="listbox"
			aria-labelledby="{scope}-label"
		>
			{#if withNone}
				<!--
				«Без прапора» — ПЕРШИЙ пункт, і він не порожній рядок на вигляд.

				Порожній пункт читається як «ще не вибрано», а це інше: тут це свідома
				відповідь «не показувати». Людина, яка не хоче називати країну, мусить
				бачити цей варіант названим, а не вгадувати.
			-->
				<CountryOption
					code=""
					name={t('pairs.countryNone')}
					id={optionId('')}
					testId="{scope}-none-option"
					chosen={value === ''}
					active={active === ''}
					{onpick}
				/>
			{/if}

			<!--
			НАЗВА ГРУПИ — в `aria-label`, а видимий заголовок `aria-hidden`.

			Не `aria-labelledby` на нього: `role="listbox"` за ARIA володіє лише
			`option` і `group`, тож заголовок усередині групи лишався б стороннім
			вузлом у дереві доступності — саме те, на що axe дає
			`aria-required-children`. Так назву чути один раз, а не двічі.
		-->
			{#each shown as group (group.id)}
				<div
					class="menu__group"
					class:menu__group--flags={flagsOnly}
					role="group"
					aria-label={group.name}
				>
					<div class="menu__region" aria-hidden="true">{@html formatFont(group.name)}</div>
					{#each group.countries as country (country.code)}
						<!--
						ПУНКТ — це `button` із роллю `option`, а не `div` з `onclick`.

						Причина не в чистоті: `div` з обробником кліку дає попередження
						компілятора `a11y_click_events_have_key_events`, а
						`svelte/valid-compile` тут стоїть у `error`. Заглушений обробник
						клавіш поруч із живим на полі пошуку був би кодом, який нічого не
						робить і виглядає, ніби робить.

						`tabindex="-1"` обов'язковий: без нього Tab ішов би по 262 кнопках,
						а керування списком лежить на полі пошуку через
						`aria-activedescendant`.
					-->
						<CountryOption
							code={country.code}
							name={country.name}
							id={optionId(country.code)}
							testId="{scope}-{country.code}-option"
							chosen={value === country.code}
							active={active === country.code}
							{flagsOnly}
							{onpick}
						/>
					{/each}
				</div>
			{/each}
		</div>
	</div>

	<!--
		«Нічого не знайдено» — ПОЗА списком, а не порожнім рядком у ньому.

		Причина та сама, що вище: у `role="listbox"` не буває вмісту, крім `option`
		і `group`. Абзац усередині нього — порушення `aria-required-children`,
		тобто читалка обіцяла б список, у якому один пункт, і він не пункт.
	-->
	{#if shown.length === 0}
		<p class="menu__empty" data-testid="{scope}-empty-text">
			{@html formatFont(text('account.countryNotFound'))}
		</p>
	{/if}
</div>

<style>
	/*
	 * ПАНЕЛЬ — над сторінкою, і саме тут теми знову щось означають: тло, рамка й
	 * колір тексту тепер НАШІ, тобто їх видно в `getComputedStyle` і їх міряє
	 * замір контрасту. Нативний випадний список не давав ні того, ні того.
	 *
	 * `z-index` вище за власну смугу прокрутки (`PageScrollbar` — 8000) і нижче
	 * за меню шапки (`HeaderMenu` — 9501): панель мусить накривати смугу, бо на
	 * сторінці акаунта дотягується майже до правого краю, і не мусить накривати
	 * меню, яке відкривають поверх усього.
	 *
	 * РОЗМІР — ІЗ ЗАМІРУ, а не з `vw` і `vh`; обидва числа приходять інлайновими
	 * змінними з `fitMenu` (`utils/menuColumns.ts`), і там же названо, чому CSS
	 * цього сам не може. Запасні значення — це рівно те, що стояло тут доти
	 * (`24rem` і `60vh`): якщо вимір не відбувся, панель лишається такою, якою
	 * була, а не зникає.
	 *
	 * Стелі `47rem` і `40rem` — не декор, а межа, за якою більше не краще.
	 * `47rem` (752px) — це рівно чотири колонки по 11rem із проміжками, і саме
	 * стільки панель бере і на 1280, і на 1920: без стелі при вікні 1920 їй було б
	 * доступно 1112px, тобто шість колонок — панель утричі ширша за саму форму
	 * (заміряно: 352px) і рядок, який око не проходить за раз. `40rem` — та сама
	 * межа для висоти: нижче панель перестає читатися як список під кнопкою.
	 *
	 * `--menu-least` лишає панель не вужчою за кнопку — це заміряна ширина самої
	 * кнопки. Доти тут стояло `min-width: 100%`, тобто ширина коробки-предка; у
	 * `<body>` такої коробки немає, і 100% означали б ширину сторінки.
	 *
	 */
	.menu {
		/*
		 * `fixed`, БО ПАНЕЛЬ ЖИВЕ В `<body>`: усередині картки її не підняти над
		 * сусідніми картками жодним `z-index` — `backdrop-filter` глобального
		 * `.text-panel` замикає власний контекст накладання. Повна причина, замір і
		 * чому не піднімається сама картка — у `utils/menuColumns.ts`.
		 *
		 * Координати ставить той самий `fitMenu` із заміру кнопки; нулі тут — щоб
		 * панель не встигла блимнути в правому нижньому куті до першого виміру.
		 */
		position: fixed;
		top: 0;
		left: 0;
		z-index: 9500;
		display: flex;
		flex-direction: column;
		gap: var(--space-xs);
		width: min(var(--menu-room, 24rem), 47rem);
		min-width: var(--menu-least, 0px);
		max-height: min(var(--menu-tall, 60vh), 40rem);
		padding: var(--space-xs);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-md);
		background: var(--color-bg-surface);
		box-shadow: var(--shadow-card-hover);
	}

	/*
	 * Поле пошуку НЕ стискається: висоту панелі тепер тримає `max-height`, і без
	 * цього рядка flex забрав би різницю саме в поля — воно ж тут єдине, що має
	 * фіксовану висоту.
	 */
	.menu__search {
		flex-shrink: 0;
		min-height: 44px;
		padding: 0 var(--space-sm);
		border: 1px solid var(--color-border);
		border-radius: var(--radius-sm);
		background: var(--color-bg-card);
		color: var(--color-text);
		font: inherit;
		font-size: var(--font-size-sm);
	}

	.menu__search:focus-visible {
		outline: 2px solid var(--color-accent);
		outline-offset: 1px;
	}

	/*
	 * ПІДКАЗКА В ПОЛІ — ПОВНИМ КОЛЬОРОМ ТЕКСТУ, і це не забудькуватість.
	 *
	 * Типова підказка браузера — `rgb(117, 117, 117)`, тобто той самий клас
	 * дефекту, від якого ми тут ідемо: колір, який не знає теми. Заміряно на
	 * `--color-bg-card`: 1.70:1 у `dark`, 1.53:1 у решти трьох при потрібних 4,5.
	 *
	 * Приглушити її НЕМА ЧИМ. Той самий замір по кандидатах:
	 *
	 * |               | dark | light-green | winter | orange-purple |
	 * | ------------- | ---- | ----------- | ------ | ------------- |
	 * | текст, 100%   | 6.23 | 5.04        | 4.67   | 5.86          |
	 * | текст, 90%    | 5.40 | 4.34        | 4.02   | 5.09          |
	 * | `--…-muted`   | 3.00 | 2.48        | 2.31   | 2.96          |
	 *
	 * Тобто вже на 90% `winter` і `light-green` падають нижче планки: у цієї теми
	 * пара «текст на картці» має всього 4,67 запасу. Тому колір повний, а «це
	 * підказка, а не введене» показує НАХИЛ — він контрасту не коштує нічого.
	 *
	 * `opacity: 1` обовʼязковий: Firefox приглушує підказку власною прозорістю
	 * поверх кольору, і без цього рядка число вище було б іншим саме там.
	 */
	.menu__search::placeholder {
		color: var(--color-text);
		opacity: 1;
		font-style: italic;
	}

	/*
	 * Висоту тримає ОБГОРТКА, а не список: чому саме так — у розмітці вище.
	 *
	 * `min-height: 0` обовʼязковий. Без нього flex-елемент не стискається нижче
	 * власного вмісту, а вміст тут від 4127px у чотирьох колонках до 11 754px в
	 * одній — тобто `max-height` на панелі не діяв би зовсім.
	 * `overscroll-behavior` не дає прокрутці «протекти» на сторінку, коли список
	 * дійшов до кінця.
	 */
	.menu__scroll {
		min-height: 0;
		overflow-y: auto;
		overscroll-behavior: contain;
	}

	/*
	 * КОЛОНКИ — `columns` із ШИРИНОЮ колонки, а не з їхньою кількістю.
	 *
	 * Кількості тут немає ні числом, ні медіа-запитом: браузер бере ширину панелі
	 * (а вона з заміру вільного місця) і сам укладає стільки колонок по 11rem,
	 * скільки влізло. Заміряно: 375px → 1 колонка, 768 → 2, 1280 → 4, 1920 → 4
	 * (упирається в стелю ширини панелі).
	 *
	 * 11rem, а не 8 і не 13. Ширше — і на 1280 колонок стає три замість чотирьох,
	 * тобто третина видимого зникає. Вужче — і переноситься не двадцятка назв, а
	 * половина списку. Заміряно, чого коштують саме 11rem: у чотирьох колонках
	 * (кожна виходить ~180px) у два рядки переходять 19 назв із 263, у три — одна
	 * («Південна Джорджія та Південні Сандвічеві Острови»); у двох колонках на
	 * 768px переносяться 5 назв, в одній на телефоні — одна.
	 *
	 * Взірець — вибір мови в `CV` (`HeaderSection.svelte`), там `columns: 150px`.
	 * Механізм той самий; різниця в масштабі, і саме вона змусила додати
	 * `break-inside` нижче.
	 */
	.menu__list {
		columns: 11rem;
		column-gap: var(--space-sm);
	}

	/*
	 * РЕЖИМ ПРАПОРІВ: multicol ВИМИКАЄТЬСЯ, а не налаштовується.
	 *
	 * Колонки й перенос рядків усередині розділу — дві розкладки на одне місце:
	 * multicol міряє висоту вмісту, а вміст тут переносить сам себе, тож браузер
	 * рвав колонки посеред сітки прапорів. Без колонок сітка розділу займає всю
	 * ширину панелі — а це і є те, чого від неї хочуть: побачити багато прапорів
	 * одразу.
	 */
	.menu__list--flags {
		columns: auto;
	}

	/*
	 * РОЗДІЛ НЕ РІЖЕТЬСЯ МІЖ КОЛОНКАМИ — і це рішення з ціною, названою числом.
	 *
	 * Без цього рядка колонки виходять ідеально рівні (11 754px на чотири =
	 * 2938px кожна), але колонка починається серединою Африки без жодного
	 * заголовка над нею: у multicol заголовок лишається там, де його застав розрив.
	 * `break-inside: avoid` це забороняє, і браузер натомість ЗБІЛЬШУЄ висоту
	 * колонки, доки цілі розділи в неї не вкладуться.
	 *
	 * Заміряно, скільки це коштує. Розділи: Європа 58, Азія 51, Африка 62,
	 * Північна Америка 45, Океанія 27, Південна Америка 14, Антарктика 5. На
	 * чотирьох колонках браузер дав 4127px замість 2938px — прокрутка на 40%
	 * довша, і близько чверті площі колонок порожня (Європа, Азія й Африка сіли по
	 * власній колонці, решта чотири — в останню).
	 *
	 * ЧОМУ НЕ БАЛАНСУВАННЯ РОЗДІЛІВ У JS. Порядок регіонів заданий
	 * (`REGION_ORDER`), тобто вибирати можна лише, де поставити три межі між
	 * сімома розділами. Найкращий такий розподіл — 4220px («решта» четвертою
	 * колонкою), тобто ГІРШЕ за те, що дає браузер сам. Код, який нічого не
	 * виправляє, не доданий.
	 *
	 * Виграш при цьому лишається великим: 11 754px прокрутки стали 4127px, а
	 * видимих без прокрутки країн — 40 замість 8 (вікно 1280×800; на 1920×1080 —
	 * 48, на телефоні 375×812 — 11 замість тих самих 8).
	 */
	.menu__group {
		break-inside: avoid;
	}

	/*
	 * СІТКА ПРАПОРІВ — flex-wrap на самому розділі, БЕЗ обгортки всередині.
	 *
	 * Обгортка була б простішою в CSS і неправильною в ARIA: у `role="group"`
	 * всередині `listbox` не буває нічого, крім `option`, і зайвий вузол дав би
	 * рівно те `aria-required-children`, через яке заголовок регіону тут
	 * `aria-hidden`, а «нічого не знайдено» лежить поза списком.
	 *
	 * Заголовок при цьому лишається окремим рядком: `flex-basis: 100%` нижче
	 * забирає йому всю ширину, тобто прапори починаються під ним, а не поруч.
	 */
	.menu__group--flags {
		display: flex;
		flex-wrap: wrap;
		gap: 2px;
		align-content: flex-start;
	}

	.menu__group--flags .menu__region {
		flex: 0 0 100%;
	}

	/*
	 * ЗАГОЛОВОК РЕГІОНУ ЛИПНЕ ДО ВЕРХУ СПИСКУ.
	 *
	 * У 262 пунктах головне питання — «де я зараз», і липкий заголовок
	 * відповідає на нього без жодної дії. Тло тут ОБОВʼЯЗКОВЕ й непрозоре:
	 * напівпрозоре давало б текст поверх назв країн, що проїжджають під ним.
	 *
	 * У КОЛОНКАХ ЦЕ ПРАЦЮЄ, І ЦЕ ЗАМІРЯНО, а не припущено: специфікація про
	 * `sticky` всередині multicol не каже нічого певного. Chrome липне ПОКОЛОНКОВО
	 * — прокрутивши список до середини, заміряно чотири заголовки на нулі й на
	 * різних абсцисах (485, 673, 860, 1048), тобто кожна колонка тримає свій.
	 * Якщо десь цього не буде, заголовок просто поїде разом зі списком; нічого не
	 * зламається й не накриє чужий текст.
	 */
	.menu__region {
		position: sticky;
		top: 0;
		z-index: 1;
		padding: var(--space-xs) var(--space-sm);
		background: var(--color-bg-surface);
		color: var(--color-text-muted);
		font-size: var(--font-size-xs);
		font-weight: var(--font-weight-bold);
		text-transform: uppercase;
		letter-spacing: 0.04em;
	}

	.menu__empty {
		margin: 0;
		padding: var(--space-sm);
		color: var(--color-text);
		font-size: var(--font-size-sm);
	}
</style>

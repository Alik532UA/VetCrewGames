<script lang="ts">
	import { t, formatFont } from '$lib/i18n';
	import PlayerBadge from './PlayerBadge.svelte';

	/**
	 * Позначка «це ви» біля власного імені.
	 *
	 * ## Що тут було й чому це не працювало
	 *
	 * Стояла крапка: `{member.name}{member.uid === me ? ' •' : ''}`. Автор сказав
	 * про неї точно — «дивно, не очевидно й не красиво», — і причина конкретна:
	 * крапка нічого не означає. Її треба РОЗГАДАТИ, а розгадувати доводиться саме
	 * тоді, коли в списку двоє незнайомців і питання «хто з них я» найважливіше.
	 * Скрінрідер при цьому читав її як «bullet» або мовчав узагалі.
	 *
	 * ## Як це роблять в іграх
	 *
	 * Найпоширеніше — СЛОВО в дужках біля імені: «(You)» у Jackbox, Kahoot,
	 * лобі Steam, таблицях результатів мережевих ігор. Рідше — підсвітка рядка
	 * власним кольором.
	 *
	 * Узято слово, і не через частоту, а тому що воно єдине працює в обох
	 * каналах: підсвітка кольором нічого не каже скрінрідеру й зникає для тих,
	 * хто не розрізняє відтінки (WCAG 1.4.1 — колір не буває єдиним носієм). Тому
	 * слово тут головне, а приглушення — лише підказка.
	 *
	 * ## Чому окремий компонент на п'ять рядків
	 *
	 * Позначка стоїть у ТРЬОХ місцях: склад лобі, табло партії та рядок
	 * переможця. Три копії розійдуться на першій же правці, і розійдуться
	 * непомітно — кожна виглядатиме правильною окремо.
	 */
</script>

<PlayerBadge tone="me">{@html formatFont(t('pairs.you'))}</PlayerBadge>
